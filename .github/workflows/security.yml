# Supply chain and static analysis: dependency audit, CodeQL (JS), PR dependency review.

name: Security

run-name: Security — ${{ github.ref_name }}

on:
  pull_request:
    branches: [main]
    paths-ignore:
      - "**.md"
      - "docs/**"
      - ".versionrc.json"
      - "AGENTS.md"
      - "CONTRIBUTING.md"
  push:
    branches: [main]
    paths-ignore:
      - "**.md"
      - "docs/**"
      - ".versionrc.json"
      - "AGENTS.md"
      - "CONTRIBUTING.md"
  schedule:
    - cron: "0 0 * * 1"
  workflow_dispatch:

# cancel-in-progress is intentionally omitted: scheduled security scans must
# not be interrupted by a concurrent push to main.
concurrency:
  group: security-${{ github.ref }}

defaults:
  run:
    shell: bash

permissions:
  contents: read

jobs:
  pnpm-audit:
    name: Dependency audit
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node and pnpm
        uses: ./.github/actions/setup-node

      - name: pnpm audit --audit-level=high
        run: pnpm audit --audit-level=high

      - name: Job summary — audit
        if: success()
        run: |
          cat << EOF >> "$GITHUB_STEP_SUMMARY"
          ## Dependency audit passed
          \`pnpm audit --audit-level=high\` — tighten or upstream-only exceptions if this fails later.
          EOF

  codeql:
    name: CodeQL (JavaScript)
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: read
      security-events: write
      actions: read
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v4
        with:
          languages: javascript
          # build-mode: none skips autobuild — correct for JS/TS (interpreted).
          build-mode: none
          # security-extended adds ~60 extra CWE checks on top of the default suite.
          queries: security-extended

      - name: Analyze with CodeQL
        uses: github/codeql-action/analyze@v4
        with:
          category: /language:javascript

  dependency-review:
    name: PR dependency diff
    runs-on: ubuntu-latest
    timeout-minutes: 5
    if: github.event_name == 'pull_request'
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Review license / severity deltas
        uses: actions/dependency-review-action@v5
        with:
          fail-on-severity: moderate
          # Post a summary comment on the PR with the dependency diff.
          comment-summary-in-pr: always
          # Allowlist approach (replaces deprecated deny-licenses): only explicitly
          # permissive / weak-copyleft licenses pass. Strong copyleft (GPL, AGPL,
          # SSPL, EUPL, BUSL) and unknown licenses are not listed — unknowns remain
          # a warning only and never block merging (per dependency-review-action).
          allow-licenses: >-
            MIT, MIT-0, Apache-2.0,
            BSD-2-Clause, BSD-3-Clause,
            ISC, 0BSD, CC0-1.0, Unlicense, Zlib,
            MPL-2.0, BlueOak-1.0.0,
            OFL-1.1, WTFPL, Python-2.0,
            MIT OR Apache-2.0,
            (MIT OR CC0-1.0),
            (WTFPL OR MIT),
            WTFPL OR ISC,
            FSL-1.1-MIT, CC-BY-4.0,
            LGPL-3.0-or-later
